# Source-local build output stays ignored. The single-file production bundle
# under web-dist/ is intentionally tracked because Helm packages it into the
# controller ConfigMap for GitOps/Argo deployments.
web/dist/
web/node_modules/
__pycache__/
*.pyc
